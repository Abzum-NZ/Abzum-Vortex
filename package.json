{
  "name": "@vortex/workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": "24"
  },
  "workspaces": [
    "apps/*",
    "contracts",
    "db",
    "runtime/*",
    "ui",
    "studio",
    "modules",
    "testing"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=@vortex/web",
    "auth:local:prepare": "node tooling/supabase/ensure-local-signing-key.mjs",
    "auth:local:proof": "pnpm auth:local:prepare && node runtime/identity/tooling/local-auth-proof.mjs",
    "db:start": "pnpm auth:local:prepare && supabase start",
    "db:stop": "supabase stop",
    "db:reset": "pnpm auth:local:prepare && supabase db reset",
    "db:new": "pnpm auth:local:prepare && supabase migration new",
    "db:test": "pnpm auth:local:prepare && supabase test db",
    "db:lint": "pnpm auth:local:prepare && supabase db lint --schema public,vortex_context --level warning --fail-on error",
    "db:verify": "pnpm db:reset && pnpm db:test && pnpm db:lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "turbo run typecheck",
    "boundaries": "node tooling/boundaries/check.mjs",
    "test": "vitest run",
    "fixtures": "vitest run testing/fixtures/validate-fixtures.test.ts",
    "build": "turbo run build",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm boundaries && pnpm test && pnpm fixtures && pnpm build"
  },
  "devDependencies": {
    "@eslint/js": "9.39.5",
    "@types/node": "24.13.3",
    "eslint": "9.39.5",
    "next": "16.3.3",
    "prettier": "3.9.6",
    "supabase": "catalog:",
    "turbo": "2.10.12",
    "typescript": "5.9.3",
    "typescript-eslint": "8.69.0",
    "vitest": "4.1.11"
  }
}
